[project]
name = "exp-23-random-stim-per-cell-14px-patches"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["faro"]

[tool.uv.sources]
# Best-effort pin: the faro main commit that last touched this experiment
# before it was migrated out of the faro repo (2026-09). A uv.lock is
# created on first revival (uv sync), not at migration. NOTE: this
# experiment is still active in the faro-pfs worktree; sync this folder
# from there when that work lands.
faro = { git = "https://github.com/pertzlab/faro.git", rev = "d366a18e223190e02ab0c3748da5101f511e77d2" }
# During active on-scope development, point at a local worktree instead:
# faro = { path = "../../faro", editable = true }
